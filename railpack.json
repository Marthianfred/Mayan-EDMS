{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "python": "3.10"
  },
  "buildAptPackages": ["libldap2-dev", "libsasl2-dev"],
  "deploy": {
    "aptPackages": [
      "libcairo2",
      "pango1.0-tools"
    ],
    "startCommand": "python manage.py initialsetup --force ; python manage.py performupgrade ; python manage.py autoadmin_create ; python manage.py dependencies_install ; python manage.py appearance_prepare_static --noinput ; gunicorn mayan.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
